import 'package:flutter/material.dart';

class MeccaTabView extends StatelessWidget {
  const MeccaTabView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Mecca"),
    );
  }
}
